// useBackend: thin wrapper for actor access
// The backend interface is currently minimal; this file
// provides a stable import point as the backend evolves.

export { useActor } from "@caffeineai/core-infrastructure";
